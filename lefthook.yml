# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md
#
# pre-push:
#   commands:
#     packages-audit:
#       tags: frontend security
#       run: yarn audit
#     gems-audit:
#       tags: backend security
#       run: bundle audit

pre-commit:
  parallel: true
  commands:
    govet:
      tags: backend style
      glob: "*.go"
      run: go vet {staged_files}
    lint:
      tags: lint
      glob: "*.go"
      run: make lint {staged_files}

#   scripts:
#     "hello.js":
#       runner: node
#     "any.go":
#       runner: go run
